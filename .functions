#!/bin/bash

rtma() {
    ssh -A -t $1 "tmux attach || tmux new"
}

pyrg() {
    rg --type python "$@"
}

jsrg() {
    rg --type js "$@"
}

gcd() {
    local repo=$(git rev-parse --show-toplevel)
    if [ $# -eq 0 ]; then
        cd $repo
    else 
        cd $repo && cd "$@"
    fi
}
